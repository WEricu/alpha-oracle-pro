[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python service.py"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
watchPatterns = ["*.py", "requirements.txt", "Procfile", "railway.toml"]
